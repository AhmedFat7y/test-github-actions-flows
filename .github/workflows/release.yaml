name: Release

on:
  push:
    branches:
      - master

jobs:
  Deploy:
    runs-on: ubuntu-latest
    steps:
      - id: deploy
        name: deploy
        run: |
          echo "deploy stuff"