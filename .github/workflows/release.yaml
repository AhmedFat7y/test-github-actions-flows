name: Release

on:
  push:
    branches:
      - main

jobs:
  Deploy-Staging:
    environment: staging
    runs-on: ubuntu-latest
    steps:
      - id: deploy
        name: deploy
        run: |
          echo "deploy stuff"
  Deploy-Production:
    environment: production
    runs-on: ubuntu-latest
    steps:
      - id: deploy
        name: deploy
        run: |
          echo "deploy stuff"
