name: Tests

on:
  push:

jobs:
  test:
    if: ${{ fromJson(vars.HELLO)[github.ref_name] != null }}
    runs-on: ubuntu-latest
    steps:
      - id: test
        name: test
        run: |
          echo ${{ github.event.before }}
      - name: Create Atlantis Commit Status
        uses: actions/github-script@v6
        with:
          script: |
            console.log(JSON.stringify(context))
